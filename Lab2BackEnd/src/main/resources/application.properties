spring.application.name=Lab2

DB_NAME=${DB_NAME}
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/${DB_NAME}}
spring.datasource.username=${DATABASE_USER}
spring.datasource.password=${DATABASE_PASSWORD}


server.port=8081

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=xd
# <--- IMPORTANT: Change this to your MongoDB database name

# If you have authentication for MongoDB:
# spring.data.mongodb.username=your-mongo-username
# spring.data.mongodb.password=your-mongo-password
# spring.data.mongodb.authentication-database=admin # Usually 'admin' or the database where the user is defined